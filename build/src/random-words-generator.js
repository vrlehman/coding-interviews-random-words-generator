export class RandomWordsGenerator {
    generate(words) {
        if (words === null || words === undefined || words.length === 0)
            return undefined;
        return "random_word2";
    }
}
